
  # Create Prototype Settings

  This is a code bundle for Create Prototype Settings. The original project is available at https://www.figma.com/design/nYZ5TQIHs67LEfy6XSmwZx/Create-Prototype-Settings.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  